name: Build Magisk Module

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      picoclaw_version:
        description: 'PicoClaw version to build (e.g., v0.2.9, nightly)'
        required: false
        default: 'nightly'
        type: string

env:
  PICOCLAW_VERSION: ${{ github.event.inputs.picoclaw_version || 'nightly' }}

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Download official picoclaw binary
        run: |
          wget -q "https://github.com/sipeed/picoclaw/releases/download/${PICOCLAW_VERSION}/picoclaw_Linux_arm64.tar.gz" -O picoclaw.tar.gz
          tar -xzf picoclaw.tar.gz
          chmod +x picoclaw picoclaw-launcher picoclaw-launcher-tui
          ls -la picoclaw picoclaw-launcher
          
      - name: Create module directory
        run: |
          mkdir -p picoclaw-module
          cp picoclaw picoclaw-module/
          cp picoclaw-launcher picoclaw-module/
          cp picoclaw-launcher-tui picoclaw-module/
          cp module.prop picoclaw-module/
          cp service.sh picoclaw-module/
          cp post-fs-data.sh picoclaw-module/
          cp customize.sh picoclaw-module/
          cp action.sh picoclaw-module/
          cp tool.sh picoclaw-module/
          cp uninstall.sh picoclaw-module/
          cp config.json picoclaw-module/
          ls -la picoclaw-module/
          
      - name: Create ZIP
        run: |
          cd picoclaw-module
          zip -r ../picoclaw-magisk-${PICOCLAW_VERSION}.zip .
          cd ..
          ls -la picoclaw-magisk-*.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: picoclaw-magisk-${{ env.PICOCLAW_VERSION }}
          path: picoclaw-magisk-${{ env.PICOCLAW_VERSION }}.zip
          retention-days: 5
          
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: picoclaw-magisk-${{ env.PICOCLAW_VERSION }}
          path: .
          
      - name: Create Release
        run: |
          # Find the downloaded zip file
          ZIP_FILE=$(ls picoclaw-magisk-*.zip | head -1)
          # Delete existing release if any
          gh release delete ${{ github.ref_name }} --yes || true
          # Create new release
          gh release create ${{ github.ref_name }} "$ZIP_FILE" --title "PicoClaw Magisk ${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
